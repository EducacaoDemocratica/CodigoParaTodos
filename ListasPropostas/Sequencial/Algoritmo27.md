# Exercício 27

[**Ver Análise**](Analise27.md)

```markdown
Algoritmo "Q27 - Sequencial"
Var
   a, b, c, x, y, d: real
Inicio
   escreva("Insira o valor de a: ")
   leia(a)
   escreva("Insira b: ")
   leia(b)
   escreva("Insira c: ")
   leia(c)
   escreval
   escreva("Insira o valor de x: ")
   leia(x)
   escreva("Insira y: ")
   leia(y)
   escreval
   d <- (a * x + b * y + c)/((a^2 + b^2)^(1/2))
   escreval("A distância de um ponto A a uma reta s é", d, ".")
Fimalgoritmo
